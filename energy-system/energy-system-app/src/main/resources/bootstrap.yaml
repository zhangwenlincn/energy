server:
  port: 8181
spring:
  application:
    name: energy-system
  cloud:
    nacos:
      discovery:
        server-addr: nacos.energy.com:8848
        namespace: energy
        group: app
      config:
        server-addr: nacos.energy.com:8848
        namespace: energy
        group: app